import * as React from "react";
import {Div} from '../utils/styles'
import {} from '../utils/lotties'
interface Props {
    
}

export default function connections({}: Props): React.ReactElement {
    return (
        <Div>
          
        </Div>
    )
}
